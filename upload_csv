import Pkg

Pkg.add("DataFrames")
Pkg.add("CSV")

using DataFrames
using CSV

filepath = "insert your own path\\fashion_boutique_dataset.csv"

df = CSV.read(filepath, DataFrame)
println(first(df, 5))
